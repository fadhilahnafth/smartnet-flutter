import 'package:flutter/material.dart';

class SensorSuhuPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Detail Sensor Suhu")),
      body: Center(child: Text("Halaman detail suhu")),
    );
  }
}
